import unittest

class TestStringMethods(unittest.TestCase):

    def test_basic(self): #Note that test methods must start with `test`
        self.assertTrue(True)

if __name__ == '__main__':
    unittest.main()
